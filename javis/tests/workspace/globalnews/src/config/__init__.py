"""Configuration management: centralized constants and settings.

Provides:
    - Shared constants (timeouts, paths, retry counts, batch sizes)
    - Path management for data directories
    - Default configuration values

Modules:
    constants - All shared constants used across the system
"""
