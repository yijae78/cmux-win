# cmux-win Phase 1: Foundation — 통합 구현 계획 v3

> **For Claude:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to implement this plan task-by-task.
> **이 문서가 유일한 구현 계획 정본(SOT)이다.**
> **설계안 정본:** `2026-03-19-cmux-win-design-v3.md` (승인 완료)

**Goal:** Electron 앱이 터미널을 띄우고, SOT로 상태를 관리하며, TCP Socket으로 외부 제어가 가능한 최소 동작 상태를 구축한다.

**Architecture:** Electron 33 Main Process에 EventEmitter+Immer 기반 SOT 스토어(6단계 미들웨어 체인)를 두고, Renderer에서 React 18.3+xterm.js로 터미널을 렌더링한다. node-pty는 preload에서 직접 실행하여 IPC 없이 키 입력 지연을 최소화한다. TCP localhost:19840에서 JSON-RPC v2 Socket API를 노출한다.

**Tech Stack (설계안 v3 섹션 4.1과 일치):**
- Electron 33 (고정)
- TypeScript 5.4+ (strict mode 전체 ON)
- React 18.3 (Concurrent 비활성, xterm.js DOM 직접 조작 호환)
- @xterm/xterm 5.x + @xterm/addon-webgl + @xterm/addon-fit
- node-pty 1.x (preload에서 직접 실행)
- Immer (불변 상태 관리)
- Zod 3.x (런타임 검증)
- Vitest (유닛/통합), Playwright (E2E)
- electron-vite (빌드)

---

## 설계안 ↔ 구현 계획 매핑표

이 표는 설계안 v3의 모든 Phase 1 관련 항목이 구현 계획의 어느 Task에 매핑되는지 보여준다.

| 설계안 섹션 | 항목 | 구현 Task |
|------------|------|----------|
| §4.1 기술 스택 | Electron 33, React 18.3, TS 5.4 strict | Task 1 |
| §5.1 Renderer 직접 PTY | preload에서 node-pty, contextBridge | Task 5 |
| §5.3 ConPTY 폴백 | winpty 자동 폴백 | Task 5 |
| §7.1 SOT 상태 구조 | AppState 전체 타입 | Task 2 |
| §7.1 스키마 버전 | PersistedState { version, state } | Task 2 |
| §7.2 스키마 마이그레이션 | Migration 체인 + 백업 | Task 4 |
| §7.3 Typesafe IPC | IPC 계약, Zod 양방향 검증 | Task 7 |
| §7.4 멀티윈도우 SOT 구독 | windowId 기반 필터링 | Task 7 |
| §3 미들웨어 6단계 | Validation→Mutation→SideEffects→Persistence→IPCBroadcast→AuditLog | Task 3+4 |
| §9.1 TCP localhost | 소켓 서버, 포트 충돌 자동 해결 | Task 6 |
| §9.2 명령 체계 | system/window/workspace/surface/agent/notification | Task 6 |
| §10.1 성능 예산 | 키 입력 ≤10ms, IPC ≤5ms | Task 5+9 |
| §11 에러 복구 | PTY 재시작, 세션 복원 실패 처리 | Task 4+9 |
| §19 개발 워크플로우 | npm run dev, test, build | Task 1 |

---

## 누적 BUG 수정 (9건 + 11건 불일치 해소)

| # | 원인 | 수정 |
|---|------|------|
| BUG-1 | preload 2개 → 1개만 로드 | `preload/index.ts` 통합 |
| BUG-2 | window.create Action 없음 | actions.ts + store.ts에 추가 |
| BUG-3 | 포트 0 반환 버그 | listen()이 실제 바인딩 포트 반환 |
| BUG-4 | PtyBridge가 electron 의존 | `main/terminal/pty-bridge.ts`로 분리 |
| BUG-5 | package.json main 없음 | `"main": "dist/main/index.js"` |
| BUG-6 | preload entry 불일치 | `preload/index.ts`로 통일 |
| BUG-7 | xterm.js v5 패키지명 | `@xterm/xterm`으로 변경 |
| BUG-8 | onWindowId 미노출 | preload + ipc-channels에 추가 |
| BUG-9 | sliceMap에 window 누락 | `window: 'windows'` 추가 |
| GAP-A | React 버전 불일치 | 18.2 → 18.3 (설계안과 일치) |
| GAP-B | 미들웨어 2개만 구현 | 6단계 전체 구현 |
| GAP-C | 소켓 핸들러 4개만 구현 | window/panel/settings 핸들러 추가 |
| GAP-D | 파일 구조 불일치 | 설계안 §17과 완전 일치 |
| GAP-E | 에러 복구 없음 | recovery-manager.ts 추가 |
| GAP-F | 스키마 마이그레이션 코드 없음 | migrations.ts 완전 구현 |
| GAP-G | IPC Broadcast가 미들웨어 아님 | 미들웨어로 재구현 |
| GAP-H | constants.ts 코드 없음 | 전체 상수 명시 |
| GAP-I | 성능 벤치마크 없음 | Task 9에 벤치마크 추가 |
| BUG-10 | post 미들웨어 예외 시 체인 중단 → UI 멈춤 | 각 미들웨어 호출을 try-catch로 감싸기 |
| BUG-11 | IpcBroadcast가 electron import → 테스트 실패 | BroadcastTarget 인터페이스로 의존성 주입 |
| BUG-12 | SideEffects 이벤트를 아무도 안 들음 | EventEmitter 상속 제거, 콜백 기반 |
| BUG-13 | XTermWrapper fontSize/fontFamily 변경 미반영 | 별도 useEffect 추가 |
| BUG-14 | 세션 복원 시 윈도우 중복 + 워크스페이스 고아 | 복원 시 windows 비우고 adoptOrphanWorkspaces 호출 |
| BUG-15 | App.tsx onWindowId 콜백 시그니처 불일치 → 빈 화면 | `(id) => setWindowId(id)` + cleanup 반환 |
| BUG-16 | index.ts 미사용 import 4건 → tsc strict 에러 | ipcMain 제거, AppState 추가, recoveryManager 제거 |
| BUG-17 | useDispatch.ts 미사용 IPC_CHANNELS import | 해당 import 제거 |
| BUG-18 | panel.ts, settings.ts 핸들러 코드 미제공 | 코드 명시 (아래 참조) |
| GAP-J | node-pty 빌드 전제 조건 미언급 | Task 1 Step 0 추가 |
| GAP-K | tsconfig.preload.json에 main/terminal/ 미포함 | include에 추가 |
| BUG-19 | node-pty 빌드 전제 조건 불충분 (GAP-J 보완) | VS 2022 C++ Desktop, Python 3.10+, electron-rebuild 대안, 검증 기준 명시 |
| BUG-20 | ConPTY 셸 호환성 매트릭스 누락 | 셸별 ConPTY/winpty 매트릭스 + PtyBridge 자동 백엔드 선택 |
| BUG-21 | preload 직접 PTY 보안 경계 미명시 | spawn/write/resize/kill만 노출, shellPath 화이트리스트 검증 |
| BUG-22 | 세션 복원 순서 경합 (BUG-14 보완) | windows=[]→복원→await 윈도우 생성→adoptOrphanWorkspaces 1회 |
| GAP-L | E2E 스모크 테스트 검증 기준 부족 | 5개 필수 시나리오 명시 |

---

## 파일 구조 (설계안 v3 §17과 일치)

```
src/
├── main/
│   ├── index.ts                           # 앱 진입점
│   ├── sot/
│   │   ├── store.ts                       # AppStateStore (6단계 미들웨어)
│   │   ├── create-default-state.ts
│   │   ├── middleware/
│   │   │   ├── validation.ts              # 1단계: Zod 검증
│   │   │   ├── side-effects.ts            # 3단계: PTY write 등
│   │   │   ├── persistence.ts             # 4단계: 디바운스 JSON 저장
│   │   │   ├── ipc-broadcast.ts           # 5단계: Renderer 동기화
│   │   │   └── audit-log.ts               # 6단계: DEBUG 로그
│   │   └── migrations/
│   │       └── index.ts                   # 스키마 마이그레이션 체인
│   ├── terminal/
│   │   └── pty-bridge.ts                  # PtyBridge (순수 Node.js)
│   ├── socket/
│   │   ├── server.ts                      # TCP 서버
│   │   ├── router.ts                      # JSON-RPC 라우터
│   │   └── handlers/
│   │       ├── system.ts                  # system.ping/identify/capabilities
│   │       ├── window.ts                  # window.list/create/close
│   │       ├── workspace.ts               # workspace.list/create/select/close/rename
│   │       ├── panel.ts                   # panel.list/focus/close (split은 Phase 2)
│   │       ├── surface.ts                 # surface.list/create/close/focus/send_text
│   │       ├── agent.ts                   # agent.session_start/status_update/session_end
│   │       ├── notification.ts            # notification.create/list/clear
│   │       └── settings.ts               # settings.get/update
│   ├── ipc/
│   │   └── handlers.ts                   # IPC 핸들러 등록
│   ├── window/
│   │   └── window-manager.ts             # BrowserWindow ↔ windowId 매핑
│   ├── error-recovery/
│   │   └── recovery-manager.ts           # PTY 재시작, 세션 복원 실패 처리
│   └── performance/
│       └── monitor.ts                    # 키 입력 지연 측정 (DEBUG)
├── preload/
│   └── index.ts                          # 통합: cmuxIpc + ptyBridge + cmuxWindowId
├── renderer/
│   ├── index.html
│   ├── main.tsx
│   ├── App.tsx
│   ├── components/terminal/
│   │   └── XTermWrapper.tsx
│   └── hooks/
│       ├── useDispatch.ts
│       └── useAppState.ts
├── shared/
│   ├── types.ts                          # 설계안 §7.1 전체 타입
│   ├── schemas.ts                        # Zod 스키마
│   ├── actions.ts                        # window.create/close 포함 전체 Action
│   ├── ipc-channels.ts                   # WINDOW_ID 포함
│   └── constants.ts                      # SCHEMA_VERSION 등 전체 상수
└── cli/
    ├── cmux-win.ts
    └── socket-client.ts

tests/
├── unit/
│   ├── shared/schemas.test.ts
│   ├── shared/actions.test.ts
│   ├── sot/store.test.ts
│   ├── sot/store-middleware.test.ts
│   ├── sot/middleware/validation.test.ts
│   ├── sot/middleware/side-effects.test.ts
│   ├── sot/middleware/persistence.test.ts
│   ├── sot/middleware/ipc-broadcast.test.ts
│   ├── sot/middleware/audit-log.test.ts
│   ├── sot/migrations/migrations.test.ts
│   ├── terminal/pty-bridge.test.ts
│   ├── socket/router.test.ts
│   ├── ipc/channels.test.ts
│   └── error-recovery/recovery.test.ts
├── integration/
│   └── socket/server.test.ts
├── e2e/
│   └── phase1-smoke.spec.ts
└── performance/
    └── key-latency.bench.ts             # 키 입력 지연 벤치마크
```

---

## Task 의존성 그래프

```
Task 1 (프로젝트 초기화 — React 18.3, @xterm/xterm)
  │
  └─→ Task 2 (shared 타입 + Zod + Action + constants)
        │
        ├─→ Task 3 (SOT 스토어 — 6단계 미들웨어 체인, window.create)
        │     │
        │     ├─→ Task 4 (미들웨어 구현 + 마이그레이션 + 에러복구)
        │     │     │
        │     │     └─→ Task 7 (Typesafe IPC + 통합 preload + 윈도우매니저)
        │     │           │
        │     │           └─→ Task 9 (통합 + E2E + 벤치마크)
        │     │
        │     └─→ Task 5 (터미널 엔진 — PtyBridge 분리, @xterm/xterm)
        │           │
        │           └─→ Task 9
        │
        └─→ Task 6 (Socket API — 7개 핸들러 카테고리, 포트 수정)
              │
              └─→ Task 8 (CLI)
                    │
                    └─→ Task 9
```

---

## Task 1: 프로젝트 초기화

### Step 0: 빌드 환경 전제 조건 확인 (GAP-J + BUG-19)

node-pty는 C++ 네이티브 모듈이므로 아래 전제 조건이 **모두** 충족되어야 한다.
하나라도 누락되면 `npm install` 또는 `postinstall` 단계에서 실패한다.

**(1) Visual Studio Build Tools 2022 — C++ Desktop workload 필수**

```bash
# 설치 확인: Developer Command Prompt에서
cl.exe
# 'cl' is not recognized → 미설치

# 설치:
# https://visualstudio.microsoft.com/visual-cpp-build-tools/
# 설치 시 "C++ 데스크톱 개발" 워크로드를 반드시 선택
# 포함 항목: MSVC v143, Windows SDK, CMake tools
```

**(2) Python 3.10+ 필수 (node-gyp 의존)**

```bash
python --version
# Python 3.10.0 이상이어야 함
# 없으면: https://www.python.org/downloads/
# 설치 시 "Add Python to PATH" 반드시 체크
```

**(3) npm에 Visual Studio 버전 명시 (npm install 전에 실행)**

```bash
npm config set msvs_version 2022
```

**(4) postinstall electron-rebuild 실패 시 대안**

package.json의 postinstall은 `electron-rebuild -f -w node-pty`를 실행한다.
실패할 경우 아래 명시적 명령으로 대체:

```bash
npx electron-rebuild -f -w node-pty -m . --arch x64
```

아키텍처가 ARM64인 경우:
```bash
npx electron-rebuild -f -w node-pty -m . --arch arm64
```

**(5) 검증 기준: node-pty 빌드 성공 확인**

```bash
node -e "const pty = require('node-pty'); const p = pty.spawn('powershell.exe', []); console.log('PID:', p.pid); p.kill(); console.log('OK');"
```
Expected: `PID: <숫자>` + `OK` 출력, 에러 없음.
실패 시: 위 (1)~(4) 단계를 재확인.

### 의존성 (설계안 §4.1과 일치)

```bash
npm install electron@33 react@18.3 react-dom@18.3 immer zod
npm install -D typescript@5.4 electron-vite vite @vitejs/plugin-react vitest eslint prettier husky lint-staged @commitlint/cli @commitlint/config-conventional @types/react @types/react-dom @electron/rebuild
```

### package.json

```json
{
  "name": "cmux-win",
  "version": "0.1.0",
  "main": "dist/main/index.js",
  "scripts": {
    "dev": "electron-vite dev",
    "build": "electron-vite build",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:coverage": "vitest run --coverage",
    "lint": "eslint src/",
    "format": "prettier --write src/",
    "bench": "vitest bench",
    "postinstall": "electron-rebuild -f -w node-pty"
  },
  "lint-staged": {
    "*.{ts,tsx}": ["eslint --fix", "prettier --write"]
  }
}
```

### TypeScript 설정

`tsconfig.json`, `tsconfig.main.json`, `tsconfig.renderer.json` — v2와 동일.

`tsconfig.preload.json` — GAP-K 수정: `src/main/terminal/` 추가:
```json
{
  "extends": "./tsconfig.json",
  "compilerOptions": {
    "module": "ESNext",
    "moduleResolution": "bundler",
    "target": "ESNext",
    "outDir": "dist/preload",
    "rootDir": "src",
    "lib": ["ESNext", "DOM"]
  },
  "include": ["src/preload/**/*", "src/shared/**/*", "src/main/terminal/**/*"]
}
```

### electron-vite.config.ts

```typescript
import { defineConfig } from 'electron-vite';
import react from '@vitejs/plugin-react';

export default defineConfig({
  main: {
    build: {
      outDir: 'dist/main',
      rollupOptions: { external: ['electron', 'node-pty', 'better-sqlite3'] },
    },
  },
  preload: {
    build: {
      outDir: 'dist/preload',
      rollupOptions: { external: ['electron', 'node-pty'] },
    },
  },
  renderer: {
    plugins: [react()],
    build: { outDir: 'dist/renderer' },
  },
});
```

### vitest.config.ts — v2와 동일.

### ESLint/Prettier/Husky — v2와 동일.

### 최소 진입점

`src/main/index.ts`, `src/preload/index.ts` (스텁), `src/renderer/index.html`, `src/renderer/main.tsx`, `src/renderer/App.tsx` — v2와 동일하되 preload 스텁에 타입만 추가:

`src/preload/index.ts` (Task 1 스텁):
```typescript
import { contextBridge } from 'electron';
contextBridge.exposeInMainWorld('cmuxWin', { platform: process.platform });
// Task 5에서 ptyBridge 추가
// Task 7에서 cmuxIpc + cmuxWindowId 추가
```

### 검증 + 커밋

```bash
npm run dev
# Expected: Electron 창에 "cmux-win" 표시

git init
printf "node_modules/\ndist/\ncoverage/\n.env\n" > .gitignore
git add -A
git commit -m "feat: initialize cmux-win (Electron 33, TS strict, React 18.3, Vitest)"
```

---

## Task 2: Shared 타입 + Zod + Action + Constants

### constants.ts (GAP-H 해소)

`src/shared/constants.ts`:
```typescript
import type { SettingsState } from './types';

export const SCHEMA_VERSION = 1;
export const DEFAULT_SOCKET_PORT = 19840;
export const MAX_SOCKET_PORT_RETRIES = 10;
export const SESSION_SAVE_DEBOUNCE_MS = 500;
export const IPC_BROADCAST_DEBOUNCE_MS = 16;
export const AGENT_PID_CHECK_INTERVAL_MS = 5000;
export const AGENT_SESSION_TTL_DAYS = 7;
export const STATE_HISTORY_MAX = 100;
export const PTY_RESTART_MAX_RETRIES = 3;
export const SESSION_BACKUP_SUFFIX = '.bak';

export const DEFAULT_SETTINGS: SettingsState = {
  appearance: { theme: 'system', language: 'system', iconMode: 'auto' },
  terminal: {
    defaultShell: 'powershell', fontSize: 14, fontFamily: 'Consolas',
    themeName: 'Dracula', cursorStyle: 'block',
  },
  browser: {
    searchEngine: 'google', searchSuggestions: true,
    httpAllowlist: ['localhost', '127.0.0.1', '::1'],
    externalUrlPatterns: [],
  },
  socket: { mode: 'automation', port: DEFAULT_SOCKET_PORT },
  agents: {
    claudeHooksEnabled: true, codexHooksEnabled: true,
    geminiHooksEnabled: true, orchestrationMode: 'auto',
  },
  telemetry: { enabled: true },
  updates: { autoCheck: true, channel: 'stable' },
  accessibility: { screenReaderMode: false, reducedMotion: false },
};
```

### ipc-channels.ts (BUG-8 포함)

`src/shared/ipc-channels.ts`:
```typescript
export const IPC_CHANNELS = {
  DISPATCH: 'cmux:dispatch',
  QUERY_STATE: 'cmux:query-state',
  GET_INITIAL_STATE: 'cmux:get-initial-state',
  STATE_UPDATE: 'cmux:state-update',
  WINDOW_ID: 'cmux:window-id',
  PTY_WRITE: 'pty:write',
  PTY_METADATA: 'pty:metadata',
} as const;
```

### types.ts — 설계안 v3 §7.1과 완전히 동일. v2 계획의 코드를 그대로 사용.

### schemas.ts — v2 계획과 동일.

### actions.ts — v2 계획과 동일 (window.create/close 포함).

### 테스트 — v2 계획과 동일.

### 커밋

```bash
npx vitest run tests/unit/shared/
git add src/shared/ tests/unit/shared/
git commit -m "feat: shared types, Zod schemas, actions (window.create/close), constants, IPC channels"
```

---

## Task 3: SOT 스토어 (6단계 미들웨어 체인) (GAP-B 해소)

### 핵심 변경: 미들웨어를 6단계 파이프라인으로 구조화

설계안 §3의 미들웨어 체인:
```
1. Validation (Zod)  → Action 검증
2. Mutation (Immer)  → 상태 변경
3. SideEffects       → PTY write, 프로세스 스폰 등
4. Persistence       → JSON 스냅샷 저장
5. IPC Broadcast     → Renderer 동기화
6. AuditLog          → DEBUG 로그
```

### store.ts — 6단계 미들웨어 지원

`src/main/sot/store.ts`:
```typescript
import { EventEmitter } from 'node:events';
import { produce } from 'immer';
import { ActionSchema, type Action } from '../../shared/actions';
import type { AppState } from '../../shared/types';
import { STATE_HISTORY_MAX } from '../../shared/constants';
import { createDefaultState } from './create-default-state';
import crypto from 'node:crypto';

export interface DispatchResult { ok: boolean; error?: string }
export interface HistoryEntry { action: Action; timestamp: number }

// 6단계 미들웨어 인터페이스 (설계안 §3)
export interface Middleware {
  // 1단계: 검증 후, 변경 전 (중단 가능)
  beforeMutation?: (action: Action, state: Readonly<AppState>) => { abort?: boolean; reason?: string };
  // 3단계: 변경 후, 사이드이펙트
  afterMutation?: (action: Action, prevState: Readonly<AppState>, nextState: Readonly<AppState>) => void;
  // 4~6단계: 비동기 후처리 (persistence, broadcast, audit)
  post?: (action: Action, prevState: Readonly<AppState>, nextState: Readonly<AppState>) => void;
}

export class AppStateStore extends EventEmitter {
  private state: AppState;
  private history: HistoryEntry[] = [];
  private middlewares: Middleware[] = [];

  constructor(initialState?: AppState) {
    super();
    this.state = initialState ?? createDefaultState();
  }

  getState(): Readonly<AppState> { return this.state; }
  getHistory(): ReadonlyArray<HistoryEntry> { return this.history; }
  use(mw: Middleware): void { this.middlewares.push(mw); }

  // BUG-14 FIX: 세션 복원 시 고아 워크스페이스를 새 윈도우에 입양
  adoptOrphanWorkspaces(windowId: string): void {
    this.state = produce(this.state, (draft) => {
      const win = draft.windows.find((w) => w.id === windowId);
      if (!win) return;
      for (const ws of draft.workspaces) {
        if (!ws.windowId || !draft.windows.some((w) => w.id === ws.windowId)) {
          ws.windowId = windowId;
          if (!win.workspaceIds.includes(ws.id)) {
            win.workspaceIds.push(ws.id);
          }
        }
      }
    });
    this.emit('change', { type: 'session.restore' });
  }

  dispatch(rawAction: unknown): DispatchResult {
    // ===== 1단계: Validation =====
    const parsed = ActionSchema.safeParse(rawAction);
    if (!parsed.success) return { ok: false, error: parsed.error.message };
    const action = parsed.data;

    // beforeMutation 미들웨어 (추가 검증, 중단 가능)
    for (const mw of this.middlewares) {
      if (mw.beforeMutation) {
        const result = mw.beforeMutation(action, this.state);
        if (result.abort) return { ok: false, error: result.reason ?? 'Aborted by middleware' };
      }
    }

    const prevState = this.state;

    try {
      // ===== 3단계: SideEffects 전용 액션 =====
      if (action.type === 'surface.send_text') {
        this.emit('side-effect', {
          type: 'pty-write',
          surfaceId: action.payload.surfaceId,
          text: action.payload.text,
        });
        for (const mw of this.middlewares) {
          try { mw.afterMutation?.(action, prevState, prevState); }
          catch (err) { console.error('[Middleware] afterMutation error:', err); }
        }
        for (const mw of this.middlewares) {
          try { mw.post?.(action, prevState, prevState); }
          catch (err) { console.error('[Middleware] post error:', err); }
        }
        return { ok: true };
      }

      // ===== 2단계: Mutation (Immer) =====
      this.state = produce(this.state, (draft) => { this.applyAction(draft, action); });

      // 히스토리
      this.history.push({ action, timestamp: Date.now() });
      if (this.history.length > STATE_HISTORY_MAX) this.history.shift();

      // ===== 3단계: afterMutation 미들웨어 (BUG-10 FIX: try-catch) =====
      for (const mw of this.middlewares) {
        try { mw.afterMutation?.(action, prevState, this.state); }
        catch (err) { console.error('[Middleware] afterMutation error:', err); }
      }

      // ===== 4~6단계: post 미들웨어 (BUG-10 FIX: try-catch) =====
      for (const mw of this.middlewares) {
        try { mw.post?.(action, prevState, this.state); }
        catch (err) { console.error('[Middleware] post error:', err); }
      }

      this.emit('change', action);
      return { ok: true };
    } catch (err) {
      return { ok: false, error: err instanceof Error ? err.message : String(err) };
    }
  }

  private applyAction(draft: AppState, action: Action): void {
    // window.create/close + workspace/panel/surface/agent/notification/focus/settings
    // v2 계획의 applyAction과 완전히 동일 — 생략하지 않고 전체 포함하되
    // 이미 작성된 코드이므로 참조: "v2 계획 Task 3 Step 2의 applyAction 전체"
    // (실제 구현 시 v2의 코드를 그대로 사용)

    switch (action.type) {
      case 'window.create': {
        const id = crypto.randomUUID();
        const geo = action.payload.geometry ?? { x: 100, y: 100, width: 1200, height: 800 };
        draft.windows.push({ id, workspaceIds: [], geometry: geo, isActive: true });
        draft.focus.activeWindowId = id;
        break;
      }
      case 'window.close': {
        const idx = draft.windows.findIndex((w) => w.id === action.payload.windowId);
        if (idx === -1) break;
        const win = draft.windows[idx];
        for (const wsId of win.workspaceIds) {
          draft.panels = draft.panels.filter((p) => p.workspaceId !== wsId);
          draft.surfaces = draft.surfaces.filter((s) => draft.panels.some((p) => p.id === s.panelId));
        }
        draft.workspaces = draft.workspaces.filter((ws) => !win.workspaceIds.includes(ws.id));
        draft.windows.splice(idx, 1);
        if (draft.focus.activeWindowId === action.payload.windowId) {
          draft.focus.activeWindowId = draft.windows[0]?.id ?? null;
        }
        break;
      }
      case 'workspace.create': {
        const id = crypto.randomUUID();
        const panelId = crypto.randomUUID();
        const surfaceId = crypto.randomUUID();
        draft.workspaces.push({ id, windowId: action.payload.windowId, name: action.payload.name ?? 'New Workspace', panelLayout: { type: 'leaf', panelId }, agentPids: {}, statusEntries: [], unreadCount: 0, isPinned: false });
        draft.panels.push({ id: panelId, workspaceId: id, panelType: 'terminal', surfaceIds: [surfaceId], activeSurfaceId: surfaceId, isZoomed: false });
        draft.surfaces.push({ id: surfaceId, panelId, surfaceType: 'terminal', title: 'Terminal' });
        const win = draft.windows.find((w) => w.id === action.payload.windowId);
        if (win) win.workspaceIds.push(id);
        break;
      }
      case 'workspace.close': {
        const wsIdx = draft.workspaces.findIndex((w) => w.id === action.payload.workspaceId);
        if (wsIdx === -1) break;
        draft.panels = draft.panels.filter((p) => p.workspaceId !== action.payload.workspaceId);
        draft.surfaces = draft.surfaces.filter((s) => draft.panels.some((p) => p.id === s.panelId));
        draft.workspaces.splice(wsIdx, 1);
        for (const win of draft.windows) { win.workspaceIds = win.workspaceIds.filter((id) => id !== action.payload.workspaceId); }
        if (draft.focus.activeWorkspaceId === action.payload.workspaceId) { draft.focus.activeWorkspaceId = draft.workspaces[0]?.id ?? null; }
        break;
      }
      case 'workspace.select': { draft.focus.activeWorkspaceId = action.payload.workspaceId; const ws = draft.workspaces.find((w) => w.id === action.payload.workspaceId); if (ws) draft.focus.activeWindowId = ws.windowId; break; }
      case 'workspace.rename': { const ws = draft.workspaces.find((w) => w.id === action.payload.workspaceId); if (ws) ws.name = action.payload.name; break; }
      case 'panel.focus': { draft.focus.activePanelId = action.payload.panelId; break; }
      case 'panel.close': { const idx = draft.panels.findIndex((p) => p.id === action.payload.panelId); if (idx === -1) break; draft.surfaces = draft.surfaces.filter((s) => s.panelId !== action.payload.panelId); draft.panels.splice(idx, 1); break; }
      case 'panel.split': break;
      case 'panel.resize': break;
      case 'surface.create': { const newId = crypto.randomUUID(); const panel = draft.panels.find((p) => p.id === action.payload.panelId); if (!panel) break; draft.surfaces.push({ id: newId, panelId: action.payload.panelId, surfaceType: action.payload.surfaceType, title: action.payload.surfaceType === 'terminal' ? 'Terminal' : 'New Tab' }); panel.surfaceIds.push(newId); panel.activeSurfaceId = newId; break; }
      case 'surface.close': { const si = draft.surfaces.findIndex((s) => s.id === action.payload.surfaceId); if (si === -1) break; const surf = draft.surfaces[si]; const panel = draft.panels.find((p) => p.id === surf.panelId); if (panel) { panel.surfaceIds = panel.surfaceIds.filter((id) => id !== action.payload.surfaceId); if (panel.activeSurfaceId === action.payload.surfaceId) panel.activeSurfaceId = panel.surfaceIds[0] ?? ''; } draft.surfaces.splice(si, 1); break; }
      case 'surface.focus': { draft.focus.activeSurfaceId = action.payload.surfaceId; const s = draft.surfaces.find((sf) => sf.id === action.payload.surfaceId); if (s) { draft.focus.activePanelId = s.panelId; const p = draft.panels.find((pp) => pp.id === s.panelId); if (p) p.activeSurfaceId = action.payload.surfaceId; } break; }
      case 'surface.send_text': break;
      case 'agent.session_start': { draft.agents.push({ sessionId: action.payload.sessionId, agentType: action.payload.agentType, workspaceId: action.payload.workspaceId, surfaceId: action.payload.surfaceId, status: 'running', statusIcon: '⚡', statusColor: 'blue', pid: action.payload.pid, lastActivity: Date.now() }); break; }
      case 'agent.status_update': { const agent = draft.agents.find((a) => a.sessionId === action.payload.sessionId); if (agent) { agent.status = action.payload.status; if (action.payload.icon) agent.statusIcon = action.payload.icon; if (action.payload.color) agent.statusColor = action.payload.color; agent.lastActivity = Date.now(); } break; }
      case 'agent.session_end': { draft.agents = draft.agents.filter((a) => a.sessionId !== action.payload.sessionId); break; }
      case 'notification.create': { draft.notifications.push({ id: crypto.randomUUID(), title: action.payload.title, subtitle: action.payload.subtitle, body: action.payload.body, workspaceId: action.payload.workspaceId, surfaceId: action.payload.surfaceId, createdAt: Date.now(), isRead: false }); break; }
      case 'notification.clear': { if (action.payload.workspaceId) { draft.notifications = draft.notifications.filter((n) => n.workspaceId !== action.payload.workspaceId); } else { draft.notifications = []; } break; }
      case 'focus.update': { const p = action.payload; if (p.activeWindowId !== undefined) draft.focus.activeWindowId = p.activeWindowId; if (p.activeWorkspaceId !== undefined) draft.focus.activeWorkspaceId = p.activeWorkspaceId; if (p.activePanelId !== undefined) draft.focus.activePanelId = p.activePanelId; if (p.activeSurfaceId !== undefined) draft.focus.activeSurfaceId = p.activeSurfaceId; if (p.focusTarget !== undefined) draft.focus.focusTarget = p.focusTarget; break; }
      case 'settings.update': { Object.assign(draft.settings, action.payload); break; }
    }
  }
}
```

### 테스트 — v2와 동일 (window.create 포함).

### 커밋

```bash
npx vitest run tests/unit/sot/store.test.ts
git add src/main/sot/store.ts src/main/sot/create-default-state.ts tests/unit/sot/
git commit -m "feat: SOT store with 6-stage middleware pipeline and window/workspace/agent CRUD"
```

---

## Task 4: 미들웨어 구현 + 마이그레이션 + 에러복구 (GAP-B,E,F,G 해소)

### 4.1 Validation 미들웨어 (1단계)

`src/main/sot/middleware/validation.ts`:
```typescript
import type { Action } from '../../../shared/actions';
import type { AppState } from '../../../shared/types';
import type { Middleware } from '../store';

export class ValidationMiddleware implements Middleware {
  beforeMutation = (action: Action, state: Readonly<AppState>): { abort?: boolean; reason?: string } => {
    // workspace.close: 존재하지 않는 워크스페이스 차단
    if (action.type === 'workspace.close') {
      if (!state.workspaces.some((ws) => ws.id === action.payload.workspaceId)) {
        return { abort: true, reason: `Workspace ${action.payload.workspaceId} not found` };
      }
    }
    // window.close: 존재하지 않는 윈도우 차단
    if (action.type === 'window.close') {
      if (!state.windows.some((w) => w.id === action.payload.windowId)) {
        return { abort: true, reason: `Window ${action.payload.windowId} not found` };
      }
    }
    // surface.close: 존재하지 않는 서피스 차단
    if (action.type === 'surface.close') {
      if (!state.surfaces.some((s) => s.id === action.payload.surfaceId)) {
        return { abort: true, reason: `Surface ${action.payload.surfaceId} not found` };
      }
    }
    return {};
  };
}
```

### 4.2 SideEffects 미들웨어 (3단계) (BUG-12 FIX: 콜백 기반)

`src/main/sot/middleware/side-effects.ts`:
```typescript
import type { Action } from '../../../shared/actions';
import type { AppState } from '../../../shared/types';
import type { Middleware } from '../store';

// BUG-12 FIX: EventEmitter 상속 제거, 콜백 기반으로 변경
// EventEmitter 사용 시 이벤트가 store가 아닌 자기 자신에게만 전달되어
// index.ts에서 수신할 수 없었음

export type SideEffect = { type: string; [key: string]: unknown };

export class SideEffectsMiddleware implements Middleware {
  private onEffect: (effect: SideEffect) => void;

  constructor(onEffect: (effect: SideEffect) => void) {
    this.onEffect = onEffect;
  }

  afterMutation = (action: Action, _prevState: Readonly<AppState>, _nextState: Readonly<AppState>): void => {
    if (action.type === 'workspace.create') {
      this.onEffect({ type: 'workspace-created', ...action.payload });
    }
    if (action.type === 'surface.close') {
      this.onEffect({ type: 'surface-closed', ...action.payload });
    }
    if (action.type === 'window.close') {
      this.onEffect({ type: 'window-closed', ...action.payload });
    }
  };
}
```

### 4.3 Persistence 미들웨어 (4단계) — v2와 동일.

### 4.4 IPC Broadcast 미들웨어 (5단계) (GAP-G + BUG-9 + BUG-11 FIX)

`src/main/sot/middleware/ipc-broadcast.ts`:
```typescript
// BUG-11 FIX: electron을 import하지 않음 — 의존성 주입으로 대체
// 이렇게 하면 Vitest에서 electron 없이 테스트 가능
import type { Action } from '../../../shared/actions';
import type { AppState } from '../../../shared/types';
import type { Middleware } from '../store';
import { IPC_CHANNELS } from '../../../shared/ipc-channels';

// electron의 BrowserWindow 대신 필요한 인터페이스만 정의
export interface BroadcastTarget {
  isDestroyed(): boolean;
  webContents: { send(channel: string, ...args: unknown[]): void };
}

export class IpcBroadcastMiddleware implements Middleware {
  private windows: Map<string, BroadcastTarget> = new Map();

  registerWindow(windowId: string, target: BroadcastTarget, onClose: (cb: () => void) => void): void {
    this.windows.set(windowId, target);
    onClose(() => this.windows.delete(windowId));
  }

  post = (action: Action, _prevState: Readonly<AppState>, nextState: Readonly<AppState>): void => {
    const slice = action.type.split('.')[0];
    const sliceMap: Record<string, string> = {
      window: 'windows',           // BUG-9 FIX
      workspace: 'workspaces',
      panel: 'panels',
      surface: 'surfaces',
      agent: 'agents',
      notification: 'notifications',
      focus: 'focus',
      settings: 'settings',
    };
    const stateKey = sliceMap[slice];
    if (!stateKey) return;

    const data = (nextState as Record<string, unknown>)[stateKey];
    for (const [windowId, win] of this.windows) {
      if (!win.isDestroyed()) {
        win.webContents.send(IPC_CHANNELS.STATE_UPDATE, { windowId, slice: stateKey, data });
      }
    }
  };
}
```

### 4.5 AuditLog 미들웨어 (6단계) — v2와 동일.

### 4.6 스키마 마이그레이션 (GAP-F)

`src/main/sot/migrations/index.ts`:
```typescript
import type { AppState, PersistedState } from '../../../shared/types';
import { SCHEMA_VERSION, SESSION_BACKUP_SUFFIX } from '../../../shared/constants';
import { createDefaultState } from '../create-default-state';
import fs from 'node:fs';

interface Migration {
  fromVersion: number;
  toVersion: number;
  migrate: (oldState: unknown) => unknown;
}

const migrations: Migration[] = [
  // 향후 스키마 변경 시 여기에 추가:
  // { fromVersion: 1, toVersion: 2, migrate: (s) => ({ ...s, newField: 'default' }) },
];

export function migrateState(persisted: PersistedState, filePath?: string): AppState {
  let { version, state } = persisted;

  // 마이그레이션 전 백업
  if (filePath && fs.existsSync(filePath)) {
    try { fs.copyFileSync(filePath, filePath + SESSION_BACKUP_SUFFIX); } catch { /* 백업 실패 무시 */ }
  }

  // 마이그레이션 체인
  for (const m of migrations) {
    if (version === m.fromVersion) {
      state = m.migrate(state) as AppState;
      version = m.toVersion;
    }
  }

  if (version !== SCHEMA_VERSION) {
    console.error(`[Migration] Cannot migrate from v${persisted.version} to v${SCHEMA_VERSION}. Using default state.`);
    return createDefaultState();
  }

  return state as AppState;
}

export function loadPersistedState(filePath: string): AppState | null {
  try {
    if (!fs.existsSync(filePath)) return null;
    const raw = fs.readFileSync(filePath, 'utf-8');
    const parsed = JSON.parse(raw) as PersistedState;
    if (!parsed || typeof parsed.version !== 'number' || !parsed.state) return null;
    return migrateState(parsed, filePath);
  } catch {
    // 손상된 파일 → 백업에서 시도
    const backupPath = filePath + SESSION_BACKUP_SUFFIX;
    try {
      if (!fs.existsSync(backupPath)) return null;
      const raw = fs.readFileSync(backupPath, 'utf-8');
      const parsed = JSON.parse(raw) as PersistedState;
      if (!parsed || typeof parsed.version !== 'number' || !parsed.state) return null;
      return migrateState(parsed);
    } catch {
      return null;
    }
  }
}
```

### 4.7 에러 복구 (GAP-E)

`src/main/error-recovery/recovery-manager.ts`:
```typescript
import type { AppStateStore } from '../sot/store';
import { PTY_RESTART_MAX_RETRIES } from '../../shared/constants';

export class RecoveryManager {
  private store: AppStateStore;
  private ptyRestartCounts: Map<string, number> = new Map();

  constructor(store: AppStateStore) {
    this.store = store;
  }

  handlePtyCrash(surfaceId: string): 'restart' | 'give-up' {
    const count = (this.ptyRestartCounts.get(surfaceId) ?? 0) + 1;
    this.ptyRestartCounts.set(surfaceId, count);

    if (count > PTY_RESTART_MAX_RETRIES) {
      this.ptyRestartCounts.delete(surfaceId);
      return 'give-up';
    }
    return 'restart';
  }

  getSurfaceRecoveryInfo(surfaceId: string): { cwd: string; shell: string } | null {
    const surface = this.store.getState().surfaces.find((s) => s.id === surfaceId);
    if (!surface?.terminal) return null;
    return { cwd: surface.terminal.cwd, shell: surface.terminal.shell };
  }

  resetRestartCount(surfaceId: string): void {
    this.ptyRestartCounts.delete(surfaceId);
  }
}
```

### 테스트 (각 미들웨어별)

각 미들웨어에 대해 v2에서 작성한 패턴과 동일한 테스트를 작성.
추가: `tests/unit/sot/migrations/migrations.test.ts`, `tests/unit/error-recovery/recovery.test.ts`.

### 커밋

```bash
npx vitest run tests/unit/sot/ tests/unit/error-recovery/
git add src/main/sot/middleware/ src/main/sot/migrations/ src/main/error-recovery/ tests/
git commit -m "feat: 6-stage middleware chain, schema migration, error recovery manager"
```

---

## Task 5: 터미널 엔진 (BUG-4,7,20,21)

### ConPTY 셸 호환성 매트릭스 (BUG-20)

```
┌─────────────────────────┬──────────┬─────────────────────────────────────┐
│ 셸                       │ 백엔드   │ 비고                                 │
├─────────────────────────┼──────────┼─────────────────────────────────────┤
│ powershell.exe          │ ConPTY   │ 완전 호환                            │
│ cmd.exe                 │ ConPTY   │ 완전 호환                            │
│ Git Bash (bash.exe)     │ winpty   │ ConPTY 사용 시 줄바꿈 깨짐 (MSYS2    │
│   C:\Program Files\     │          │ PTY 충돌). useConpty=false 필수       │
│   Git\bin\bash.exe      │          │                                     │
│ WSL (wsl.exe)           │ 별도처리  │ ConPTY 미지원. wsl.exe 래퍼 경유     │
│                         │          │ 필요 (wsl.exe 자체가 PTY 브릿지)     │
└─────────────────────────┴──────────┴─────────────────────────────────────┘
```

### PtyBridge — 셸 경로 기반 자동 백엔드 선택 (BUG-20)

`src/main/terminal/pty-bridge.ts` 수정 — spawn 메서드에 백엔드 자동 선택 추가:

```typescript
import * as pty from 'node-pty';
import os from 'node:os';
import fs from 'node:fs';
import path from 'node:path';

interface PtySpawnOptions {
  cols: number; rows: number; cwd: string; env?: Record<string, string>;
}

interface ManagedPty {
  instance: pty.IPty;
  dataCallbacks: Array<(data: string) => void>;
  exitCallbacks: Array<(code: number) => void>;
}

// BUG-21: 허용된 셸 경로 화이트리스트
const ALLOWED_SHELLS: ReadonlySet<string> = new Set([
  'powershell', 'cmd', 'wsl', 'git-bash',
  'powershell.exe', 'cmd.exe', 'wsl.exe',
  'bash.exe', 'bash',
]);

export class PtyBridge {
  private instances: Map<string, ManagedPty> = new Map();

  spawn(id: string, shell: string, args: string[], options: PtySpawnOptions): { id: string; pid: number } {
    // BUG-21: 화이트리스트 검증
    const shellBase = path.basename(shell).toLowerCase();
    const shellKey = shell.toLowerCase();
    if (!ALLOWED_SHELLS.has(shellKey) && !ALLOWED_SHELLS.has(shellBase)) {
      throw new Error(`Shell not allowed: ${shell}. Allowed: ${[...ALLOWED_SHELLS].join(', ')}`);
    }

    const resolvedShell = this.resolveShell(shell);

    // BUG-20: 셸 경로 기반 ConPTY/winpty 자동 선택
    const useConpty = this.shouldUseConpty(resolvedShell);

    const instance = pty.spawn(resolvedShell, args, {
      name: 'xterm-256color',
      cols: options.cols,
      rows: options.rows,
      cwd: options.cwd || os.homedir(),
      env: { ...process.env, ...options.env } as Record<string, string>,
      useConpty,  // BUG-20: 자동 선택
    });

    const managed: ManagedPty = { instance, dataCallbacks: [], exitCallbacks: [] };

    instance.onData((data: string) => {
      for (const cb of managed.dataCallbacks) cb(data);
    });

    instance.onExit(({ exitCode }: { exitCode: number }) => {
      for (const cb of managed.exitCallbacks) cb(exitCode);
      this.instances.delete(id);
    });

    this.instances.set(id, managed);
    return { id, pid: instance.pid };
  }

  write(id: string, data: string): void { this.instances.get(id)?.instance.write(data); }
  resize(id: string, cols: number, rows: number): void { this.instances.get(id)?.instance.resize(cols, rows); }
  kill(id: string): void { this.instances.get(id)?.instance.kill(); this.instances.delete(id); }
  has(id: string): boolean { return this.instances.has(id); }
  onData(id: string, cb: (data: string) => void): void { this.instances.get(id)?.dataCallbacks.push(cb); }
  onExit(id: string, cb: (code: number) => void): void { this.instances.get(id)?.exitCallbacks.push(cb); }

  getAvailableShells(): string[] {
    const shells: string[] = ['powershell'];
    if (fs.existsSync('C:\\Windows\\System32\\cmd.exe')) shells.push('cmd');
    if (fs.existsSync('C:\\Windows\\System32\\wsl.exe')) shells.push('wsl');
    if (fs.existsSync('C:\\Program Files\\Git\\bin\\bash.exe') || fs.existsSync('C:\\Program Files (x86)\\Git\\bin\\bash.exe')) shells.push('git-bash');
    return shells;
  }

  // BUG-20: ConPTY vs winpty 자동 선택
  private shouldUseConpty(resolvedShell: string): boolean {
    const lower = resolvedShell.toLowerCase();

    // Git Bash: MSYS2 PTY와 ConPTY 충돌 → winpty 사용
    if (lower.includes('git') && lower.includes('bash')) {
      return false;
    }

    // WSL: wsl.exe 자체가 PTY 브릿지 역할 → ConPTY 사용
    if (lower.includes('wsl')) {
      return true;
    }

    // 기본: ConPTY 사용
    return true;
  }

  private resolveShell(shell: string): string {
    switch (shell) {
      case 'powershell': return 'powershell.exe';
      case 'cmd': return 'cmd.exe';
      case 'wsl': return 'wsl.exe';
      case 'git-bash': return fs.existsSync('C:\\Program Files\\Git\\bin\\bash.exe')
        ? 'C:\\Program Files\\Git\\bin\\bash.exe'
        : 'C:\\Program Files (x86)\\Git\\bin\\bash.exe';
      default: return shell;
    }
  }
}
```

### 보안 경계 (BUG-21)

```
preload/index.ts에서 contextBridge로 노출하는 PTY API:

  노출되는 API (4개만):
    ptyBridge.spawn(id, shell, args, options)  → PtyBridge.spawn()
    ptyBridge.write(id, data)                  → PtyBridge.write()
    ptyBridge.resize(id, cols, rows)           → PtyBridge.resize()
    ptyBridge.kill(id)                         → PtyBridge.kill()
    + onData, onExit, has, getAvailableShells (읽기 전용)

  보안 제약:
    1. shellPath는 ALLOWED_SHELLS 화이트리스트로 검증 (PtyBridge 내부)
    2. Renderer에서 임의 셸 경로 지정 불가 — 'powershell', 'cmd', 'wsl', 'git-bash' 키만 허용
    3. spawn의 args 배열은 빈 배열만 허용 (Phase 1)
    4. cwd는 유효한 디렉토리 경로인지 fs.existsSync로 검증

  Renderer가 할 수 없는 것:
    - 임의 실행 파일 경로 (예: 'C:\\malware.exe') → ALLOWED_SHELLS에 없으므로 throw
    - 임의 인자 (Phase 1에서는 빈 배열 강제)
    - node-pty 인스턴스 직접 접근 (contextBridge가 프록시)
```

### XTermWrapper (BUG-7 수정)

`src/renderer/components/terminal/XTermWrapper.tsx` — import 수정:
```typescript
import { Terminal } from '@xterm/xterm';
import { WebglAddon } from '@xterm/addon-webgl';
import { FitAddon } from '@xterm/addon-fit';
import '@xterm/xterm/css/xterm.css';
```
나머지 로직 v2와 동일.

### 의존성 설치

```bash
npm install @xterm/xterm @xterm/addon-webgl @xterm/addon-fit @xterm/addon-search node-pty
npx @electron/rebuild -f -w node-pty
```

---

## Task 6: Socket API (GAP-C — 7개 핸들러 카테고리)

### 핸들러 파일 목록 (설계안 §9.2와 일치)

| 파일 | 명령 |
|------|------|
| `handlers/system.ts` | system.ping, system.identify, system.capabilities |
| `handlers/window.ts` | window.list, window.current, window.create, window.close |
| `handlers/workspace.ts` | workspace.list, workspace.current, workspace.create, workspace.select, workspace.close, workspace.rename |
| `handlers/panel.ts` | panel.list, panel.focus, panel.close |
| `handlers/surface.ts` | surface.list, surface.create, surface.close, surface.focus, surface.send_text |
| `handlers/agent.ts` | agent.session_start, agent.status_update, agent.session_end |
| `handlers/notification.ts` | notification.create, notification.list, notification.clear |
| `handlers/settings.ts` | settings.get, settings.update |

### window.ts (GAP-C — 신규)

`src/main/socket/handlers/window.ts`:
```typescript
import { JsonRpcRouter } from '../router';
import type { AppStateStore } from '../../sot/store';

export function registerWindowHandlers(router: JsonRpcRouter, store: AppStateStore): void {
  router.register('window.list', () => store.getState().windows.map((w) => ({ id: w.id, workspaceIds: w.workspaceIds, isActive: w.isActive })));
  router.register('window.current', () => {
    const id = store.getState().focus.activeWindowId;
    return id ? store.getState().windows.find((w) => w.id === id) ?? null : null;
  });
  router.register('window.create', (params: unknown) => {
    const result = store.dispatch({ type: 'window.create', payload: params ?? {} });
    if (!result.ok) throw new Error(result.error);
    return { id: store.getState().windows.at(-1)?.id };
  });
  router.register('window.close', (params: unknown) => {
    const result = store.dispatch({ type: 'window.close', payload: params });
    if (!result.ok) throw new Error(result.error);
    return { ok: true };
  });
}
```

### panel.ts (BUG-18 FIX — 코드 명시)

`src/main/socket/handlers/panel.ts`:
```typescript
import { JsonRpcRouter } from '../router';
import type { AppStateStore } from '../../sot/store';

export function registerPanelHandlers(router: JsonRpcRouter, store: AppStateStore): void {
  router.register('panel.list', (params: unknown) => {
    const p = params as { workspaceId?: string } | undefined;
    let panels = store.getState().panels;
    if (p?.workspaceId) {
      panels = panels.filter((pl) => pl.workspaceId === p.workspaceId);
    }
    return panels.map((pl) => ({ id: pl.id, workspaceId: pl.workspaceId, panelType: pl.panelType, isZoomed: pl.isZoomed }));
  });

  router.register('panel.focus', (params: unknown) => {
    const result = store.dispatch({ type: 'panel.focus', payload: params });
    if (!result.ok) throw new Error(result.error);
    return { ok: true };
  });

  router.register('panel.close', (params: unknown) => {
    const result = store.dispatch({ type: 'panel.close', payload: params });
    if (!result.ok) throw new Error(result.error);
    return { ok: true };
  });
}
```

### settings.ts (BUG-18 FIX — 코드 명시)

`src/main/socket/handlers/settings.ts`:
```typescript
import { JsonRpcRouter } from '../router';
import type { AppStateStore } from '../../sot/store';

export function registerSettingsHandlers(router: JsonRpcRouter, store: AppStateStore): void {
  router.register('settings.get', () => {
    return store.getState().settings;
  });

  router.register('settings.update', (params: unknown) => {
    const result = store.dispatch({ type: 'settings.update', payload: params ?? {} });
    if (!result.ok) throw new Error(result.error);
    return { ok: true };
  });
}
```

### server.ts (BUG-3 수정) — v2와 동일.

### 커밋

```bash
npx vitest run tests/unit/socket/ tests/integration/socket/
git add src/main/socket/ tests/
git commit -m "feat: Socket API with 8 handler categories (system/window/workspace/panel/surface/agent/notification/settings)"
```

---

## Task 7: Typesafe IPC + 통합 preload + 윈도우매니저 (BUG-1,6,8,9)

### 통합 preload (`src/preload/index.ts`)

v2의 코드에 `cmuxWindowId` 추가 (BUG-8):
```typescript
import { contextBridge, ipcRenderer } from 'electron';
import { PtyBridge } from '../main/terminal/pty-bridge';
import { IPC_CHANNELS } from '../shared/ipc-channels';

// ===== IPC Bridge =====
contextBridge.exposeInMainWorld('cmuxIpc', {
  dispatch: (action: unknown) => ipcRenderer.invoke(IPC_CHANNELS.DISPATCH, action),
  queryState: (query: { slice: string }) => ipcRenderer.invoke(IPC_CHANNELS.QUERY_STATE, query),
  getInitialState: () => ipcRenderer.invoke(IPC_CHANNELS.GET_INITIAL_STATE),
  onStateUpdate: (callback: (data: { windowId: string; slice: string; data: unknown }) => void) => {
    const handler = (_e: unknown, data: { windowId: string; slice: string; data: unknown }) => callback(data);
    ipcRenderer.on(IPC_CHANNELS.STATE_UPDATE, handler);
    return () => ipcRenderer.removeListener(IPC_CHANNELS.STATE_UPDATE, handler);
  },
});

// ===== Window ID (BUG-8 FIX) =====
contextBridge.exposeInMainWorld('cmuxWindowId', {
  onWindowId: (callback: (id: string) => void) => {
    const handler = (_e: unknown, id: string) => callback(id);
    ipcRenderer.on(IPC_CHANNELS.WINDOW_ID, handler);
    return () => ipcRenderer.removeListener(IPC_CHANNELS.WINDOW_ID, handler);
  },
});

// ===== PTY Bridge =====
const ptyBridge = new PtyBridge();
contextBridge.exposeInMainWorld('ptyBridge', {
  spawn: (id: string, shell: string, args: string[], options: { cols: number; rows: number; cwd: string }) => {
    const result = ptyBridge.spawn(id, shell, args, options);
    ipcRenderer.send(IPC_CHANNELS.PTY_METADATA, { surfaceId: id, pid: result.pid, cwd: options.cwd, shell });
    return result;
  },
  write: (id: string, data: string) => ptyBridge.write(id, data),
  resize: (id: string, cols: number, rows: number) => ptyBridge.resize(id, cols, rows),
  kill: (id: string) => ptyBridge.kill(id),
  has: (id: string) => ptyBridge.has(id),
  onData: (id: string, cb: (data: string) => void) => ptyBridge.onData(id, cb),
  onExit: (id: string, cb: (code: number) => void) => ptyBridge.onExit(id, cb),
  getAvailableShells: () => ptyBridge.getAvailableShells(),
});

// Main → Renderer: Socket API PTY write 포워딩
ipcRenderer.on(IPC_CHANNELS.PTY_WRITE, (_e, data: { surfaceId: string; text: string }) => {
  ptyBridge.write(data.surfaceId, data.text);
});

contextBridge.exposeInMainWorld('cmuxWin', { platform: process.platform });
```

### 윈도우 매니저 (GAP-D)

`src/main/window/window-manager.ts`:
```typescript
import type { BrowserWindow } from 'electron';

export class WindowManager {
  private windows: Map<string, BrowserWindow> = new Map();

  register(windowId: string, win: BrowserWindow): void {
    this.windows.set(windowId, win);
    win.on('closed', () => this.windows.delete(windowId));
  }

  get(windowId: string): BrowserWindow | undefined {
    return this.windows.get(windowId);
  }

  getAll(): Map<string, BrowserWindow> {
    return this.windows;
  }

  findByWebContentsId(webContentsId: number): string | undefined {
    for (const [windowId, win] of this.windows) {
      if (!win.isDestroyed() && win.webContents.id === webContentsId) return windowId;
    }
    return undefined;
  }
}
```

### IPC handlers — v2와 동일 + IpcBroadcastMiddleware는 이제 `sot/middleware/ipc-broadcast.ts`에 위치 (Task 4).

---

## Task 8: CLI — v2와 동일.

---

## Task 9: 통합 + E2E + 벤치마크 (GAP-I)

### main/index.ts (최종)

```typescript
import { app, BrowserWindow } from 'electron';            // BUG-16 FIX: ipcMain 제거 (handlers.ts 내부에서 import)
import path from 'node:path';
import type { AppState } from '../shared/types';           // BUG-16 FIX: AppState import 추가
import { AppStateStore } from './sot/store';
import { ValidationMiddleware } from './sot/middleware/validation';
import { SideEffectsMiddleware } from './sot/middleware/side-effects';
import { PersistenceMiddleware } from './sot/middleware/persistence';
import { IpcBroadcastMiddleware } from './sot/middleware/ipc-broadcast';
import { AuditLogMiddleware } from './sot/middleware/audit-log';
import { loadPersistedState } from './sot/migrations';
// BUG-16 FIX: RecoveryManager는 Phase 5에서 활성화 — Phase 1에서는 import하지 않음
// import { RecoveryManager } from './error-recovery/recovery-manager';
import { WindowManager } from './window/window-manager';
import { registerIpcHandlers } from './ipc/handlers';
import { SocketApiServer } from './socket/server';
import { JsonRpcRouter } from './socket/router';
import { registerSystemHandlers } from './socket/handlers/system';
import { registerWindowHandlers } from './socket/handlers/window';
import { registerWorkspaceHandlers } from './socket/handlers/workspace';
import { registerPanelHandlers } from './socket/handlers/panel';
import { registerSurfaceHandlers } from './socket/handlers/surface';
import { registerAgentHandlers } from './socket/handlers/agent';
import { registerNotificationHandlers } from './socket/handlers/notification';
import { registerSettingsHandlers } from './socket/handlers/settings';
import { IPC_CHANNELS } from '../shared/ipc-channels';

const SESSION_FILE = path.join(app.getPath('appData'), 'cmux-win', 'session.json');
const DEBUG_LOG = path.join(app.getPath('temp'), 'cmux-win-debug.log');

// --- SOT (BUG-14 FIX: 세션 복원 시 윈도우 비우기) ---
const persisted = loadPersistedState(SESSION_FILE);
let initialState: AppState | undefined;
if (persisted) {
  // 윈도우는 항상 새로 생성 — 복원 대상에서 제외
  initialState = { ...persisted, windows: [] };
}
const store = new AppStateStore(initialState);

// --- 6단계 미들웨어 등록 (설계안 §3 순서) ---
const validationMw = new ValidationMiddleware();           // 1. Validation
// 2. Mutation은 store 내장
const sideEffectsMw = new SideEffectsMiddleware((effect) => { // 3. SideEffects (BUG-12 FIX)
  // 사이드이펙트를 store의 side-effect 이벤트로 포워딩
  store.emit('side-effect', effect);
});
const persistenceMw = new PersistenceMiddleware(SESSION_FILE); // 4. Persistence
const ipcBroadcastMw = new IpcBroadcastMiddleware();       // 5. IPC Broadcast
const auditLogMw = new AuditLogMiddleware(DEBUG_LOG);      // 6. AuditLog

store.use(validationMw);
store.use(sideEffectsMw);
store.use(persistenceMw);
store.use(ipcBroadcastMw);
store.use(auditLogMw);

// --- Error Recovery ---
// BUG-16 FIX: Phase 1에서는 RecoveryManager 비활성화
// const recoveryManager = new RecoveryManager(store);

// --- Window Manager ---
const windowManager = new WindowManager();

// --- IPC ---
registerIpcHandlers(store);

// --- Side-effect: PTY write ---
store.on('side-effect', (effect: { type: string; surfaceId: string; text: string }) => {
  if (effect.type === 'pty-write') {
    for (const [_id, win] of windowManager.getAll()) {
      if (!win.isDestroyed()) {
        win.webContents.send(IPC_CHANNELS.PTY_WRITE, { surfaceId: effect.surfaceId, text: effect.text });
      }
    }
  }
});

// --- Socket API ---
const router = new JsonRpcRouter();
registerSystemHandlers(router);
registerWindowHandlers(router, store);
registerWorkspaceHandlers(router, store);
registerPanelHandlers(router, store);
registerSurfaceHandlers(router, store);
registerAgentHandlers(router, store);
registerNotificationHandlers(router, store);
registerSettingsHandlers(router, store);
const socketServer = new SocketApiServer(router);

// --- Window ---
// BUG-22 FIX: createWindow는 Promise를 반환 — did-finish-load를 await 가능
function createWindow(): Promise<{ win: BrowserWindow; windowId: string }> {
  return new Promise((resolve) => {
    const win = new BrowserWindow({
      width: 1200, height: 800, title: 'cmux-win', backgroundColor: '#1e1e1e',
      webPreferences: {
        preload: path.join(__dirname, '../preload/index.js'),
        contextIsolation: true, nodeIntegration: false, webviewTag: true,
      },
    });

    // Step 2: SOT에 윈도우 등록
    const bounds = win.getBounds();
    store.dispatch({ type: 'window.create', payload: { geometry: { x: bounds.x, y: bounds.y, width: bounds.width, height: bounds.height } } });
    const windowId = store.getState().windows.at(-1)!.id;

    windowManager.register(windowId, win);
    ipcBroadcastMw.registerWindow(windowId, win, (cb) => win.on('closed', cb)); // BUG-11 FIX

    // Step 3: BrowserWindow 로드 완료 대기
    win.webContents.on('did-finish-load', () => {
      win.webContents.send(IPC_CHANNELS.WINDOW_ID, windowId);
      resolve({ win, windowId });
    });

    if (process.env.NODE_ENV === 'development') {
      win.loadURL('http://localhost:5173');
    } else {
      win.loadFile(path.join(__dirname, '../renderer/index.html'));
    }
  });
}

// --- Lifecycle (BUG-22 FIX: 세션 복원 순서 강제) ---
//
// 순서:
//   (1) state.windows=[] 초기화 — loadPersistedState 시점에서 완료 (위 코드)
//   (2) JSON에서 워크스페이스/패널/서피스 복원 — store 생성 시 완료 (위 코드)
//   (3) BrowserWindow 생성 완료 await
//   (4) adoptOrphanWorkspaces 1회 호출 — 모든 윈도우 완료 후
//   (5) PTY 재연결 — Renderer가 XTermWrapper 마운트 시 자동
//
app.whenReady().then(async () => {
  const port = await socketServer.start();
  process.env.CMUX_SOCKET_ADDR = `tcp://127.0.0.1:${port}`;

  // Phase 1: 단일 윈도우. Phase 2에서는 Promise.all로 병렬 생성.
  const { windowId } = await createWindow();  // Step 3: await 완료

  // Step 4: 모든 윈도우 생성 완료 후 1회만 호출
  store.adoptOrphanWorkspaces(windowId);
});

app.on('window-all-closed', () => {
  persistenceMw.dispose();
  socketServer.stop();
  app.quit();
});
```

### E2E 스모크 테스트 — 필수 5개 시나리오 (GAP-L)

`tests/e2e/phase1-smoke.spec.ts`:
```typescript
import { test, expect } from '@playwright/test';
import { _electron as electron } from 'playwright';
import net from 'node:net';

function sendRpc(port: number, method: string, params?: unknown): Promise<unknown> {
  return new Promise((resolve, reject) => {
    const client = net.createConnection({ port, host: '127.0.0.1' }, () => {
      client.write(JSON.stringify({ jsonrpc: '2.0', method, params, id: 1 }) + '\n');
    });
    let buffer = '';
    client.on('data', (chunk) => {
      buffer += chunk.toString();
      if (buffer.includes('\n')) {
        client.end();
        try {
          const res = JSON.parse(buffer.trim());
          if (res.error) reject(new Error(res.error.message));
          else resolve(res.result);
        } catch (e) { reject(e); }
      }
    });
    client.on('error', reject);
    setTimeout(() => { client.destroy(); reject(new Error('Timeout')); }, 5000);
  });
}

const SOCKET_PORT = 19840;

test.describe('Phase 1 Smoke Tests (GAP-L)', () => {
  let electronApp: Awaited<ReturnType<typeof electron.launch>>;

  test.beforeAll(async () => {
    electronApp = await electron.launch({
      args: ['.'],
      env: { ...process.env, NODE_ENV: 'production' },
    });
    // 앱 초기화 대기
    await new Promise((r) => setTimeout(r, 3000));
  });

  test.afterAll(async () => {
    await electronApp.close();
  });

  // (1) 앱 실행 후 터미널 프롬프트 3초 이내 표시
  test('terminal prompt appears within 3 seconds', async () => {
    const window = electronApp.windows()[0];
    expect(window).toBeDefined();
    // xterm.js가 DOM에 마운트되었는지 확인
    const hasTerminal = await window.evaluate(() => {
      return document.querySelector('.xterm') !== null;
    });
    expect(hasTerminal).toBe(true);
  });

  // (2) 키 입력→화면 반영 10ms 이내 (performance.now 측정)
  test('key input latency under 10ms', async () => {
    const window = electronApp.windows()[0];
    const latency = await window.evaluate(() => {
      return new Promise<number>((resolve) => {
        const terminal = document.querySelector('.xterm-helper-textarea') as HTMLTextAreaElement;
        if (!terminal) { resolve(-1); return; }
        const start = performance.now();
        terminal.dispatchEvent(new KeyboardEvent('keydown', { key: 'a', code: 'KeyA' }));
        // xterm.js는 동기적으로 DOM을 업데이트하므로 즉시 측정
        requestAnimationFrame(() => {
          const end = performance.now();
          resolve(end - start);
        });
      });
    });
    // 10ms 이내 (프레임 1개 이내)
    expect(latency).toBeLessThan(10);
  });

  // (3) Socket API system.ping 응답 수신
  test('socket API responds to system.ping', async () => {
    const result = await sendRpc(SOCKET_PORT, 'system.ping') as { pong: boolean };
    expect(result.pong).toBe(true);
  });

  // (4) surface.send_text로 터미널 텍스트 출력 확인
  test('surface.send_text delivers text to terminal', async () => {
    // 워크스페이스 목록에서 첫 번째 서피스 ID 획득
    const workspaces = await sendRpc(SOCKET_PORT, 'workspace.list') as Array<{ id: string }>;
    expect(workspaces.length).toBeGreaterThan(0);

    const surfaces = await sendRpc(SOCKET_PORT, 'surface.list', { workspaceId: workspaces[0].id }) as Array<{ id: string }>;
    expect(surfaces.length).toBeGreaterThan(0);

    const surfaceId = surfaces[0].id;
    const result = await sendRpc(SOCKET_PORT, 'surface.send_text', { surfaceId, text: 'echo cmux-e2e-test\r' });
    expect(result).toHaveProperty('ok', true);

    // 잠시 대기 후 터미널 출력에 텍스트가 포함되었는지 확인
    await new Promise((r) => setTimeout(r, 1000));
    const window = electronApp.windows()[0];
    const hasOutput = await window.evaluate(() => {
      const rows = document.querySelectorAll('.xterm-rows > div');
      for (const row of rows) {
        if (row.textContent?.includes('cmux-e2e-test')) return true;
      }
      return false;
    });
    expect(hasOutput).toBe(true);
  });

  // (5) 앱 종료→재실행→이전 세션 워크스페이스 복원 확인
  test('session restore preserves workspaces', async () => {
    // 현재 워크스페이스 이름 확인
    const beforeWorkspaces = await sendRpc(SOCKET_PORT, 'workspace.list') as Array<{ name: string }>;
    expect(beforeWorkspaces.length).toBeGreaterThan(0);
    const wsName = beforeWorkspaces[0].name;

    // 앱 종료
    await electronApp.close();

    // 재실행
    electronApp = await electron.launch({
      args: ['.'],
      env: { ...process.env, NODE_ENV: 'production' },
    });
    await new Promise((r) => setTimeout(r, 3000));

    // 워크스페이스 복원 확인
    const afterWorkspaces = await sendRpc(SOCKET_PORT, 'workspace.list') as Array<{ name: string }>;
    expect(afterWorkspaces.length).toBeGreaterThan(0);
    expect(afterWorkspaces.some((ws) => ws.name === wsName)).toBe(true);
  });
});
```

### 성능 벤치마크 (GAP-I)

`tests/performance/key-latency.bench.ts`:
```typescript
import { bench, describe } from 'vitest';
import { AppStateStore } from '../../src/main/sot/store';

describe('Performance', () => {
  bench('SOT dispatch latency', () => {
    const store = new AppStateStore();
    store.dispatch({ type: 'window.create', payload: {} });
    const winId = store.getState().windows[0].id;
    store.dispatch({ type: 'workspace.create', payload: { windowId: winId, name: 'bench' } });
  });

  bench('notification create 100x', () => {
    const store = new AppStateStore();
    for (let i = 0; i < 100; i++) {
      store.dispatch({ type: 'notification.create', payload: { title: `N${i}` } });
    }
  });
});
```

### Phase 1 완료 체크리스트

```
기능:
  [ ] npm run dev → Electron 창 + xterm.js 터미널
  [ ] 터미널에서 키 입력 ≤10ms
  [ ] cmux-win ping → {"pong":true}
  [ ] cmux-win list-workspaces → JSON
  [ ] cmux-win list-windows → JSON (GAP-C)
  [ ] cmux-win notify --title "test" → 알림 생성
  [ ] 앱 종료 → 재시작 → 세션 복원
  [ ] 세션 파일 손상 → 백업에서 복원 → 실패 시 빈 상태 (GAP-E,F)

테스트:
  [ ] npx vitest run → ALL PASS
  [ ] 커버리지 ≥90% (src/main/sot/, src/shared/)
  [ ] 벤치마크: SOT dispatch ≤1ms (GAP-I)

품질:
  [ ] TypeScript strict 에러 0개
  [ ] ESLint 경고 0개
  [ ] 6단계 미들웨어 전체 동작 (GAP-B)
  [ ] BUG 1~22 전부 수정 확인
  [ ] GAP A~I 전부 해소 확인
  [ ] BUG-10: persistenceMw 예외 시에도 ipcBroadcastMw 실행됨
  [ ] BUG-11: ipc-broadcast.test.ts가 electron 없이 Vitest 통과
  [ ] BUG-12: workspace.create → sideEffectsMw → store.emit('side-effect') 도달
  [ ] BUG-13: 설정에서 fontSize 변경 → 터미널에 즉시 반영
  [ ] BUG-14: 앱 종료 → 재시작 → 윈도우 1개 + 이전 워크스페이스 모두 연결됨
  [ ] BUG-15: App.tsx에서 windowId 정상 수신, 콜백 인자 1개
  [ ] BUG-16: tsc strict 에러 0개 (미사용 import 없음)
  [ ] BUG-17: useDispatch.ts에 불필요한 import 없음
  [ ] BUG-18: panel.list/focus/close + settings.get/update 소켓 명령 동작
  [ ] GAP-J: node-pty 빌드 성공 (VS Build Tools + Python 확인)
  [ ] GAP-K: tsc --project tsconfig.preload.json 에러 없음
  [ ] BUG-19: require('node-pty').spawn('powershell.exe',[]) 에러 없이 PID 반환
  [ ] BUG-20: Git Bash에서 useConpty=false 자동 적용, 줄바꿈 정상
  [ ] BUG-21: Renderer에서 'C:\\malware.exe' spawn 시도 → "Shell not allowed" 에러
  [ ] BUG-22: 앱 재시작 시 adoptOrphanWorkspaces가 createWindow await 후 1회만 호출
  [ ] GAP-L: E2E 5개 시나리오 전체 통과 (프롬프트, 지연, ping, send_text, 세션복원)
```
